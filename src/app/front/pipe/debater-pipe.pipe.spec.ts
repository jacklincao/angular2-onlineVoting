import { DebaterPipePipe } from './debater-pipe.pipe';

describe('DebaterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DebaterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
